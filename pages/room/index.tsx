import React from 'react';

import Head from 'next/head';

import MainContainer from '../../components/MainContainer';

const Room = (): any => {
  return (
    <MainContainer>
      <Head>
        <title>Room</title>
      </Head>
      <h1>Room</h1>
    </MainContainer>
  );
};

export default Room;
